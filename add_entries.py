# This script specifies functions that add entries 
from shiny import module, ui, render, reactive, event

@module.ui
def add_entries_ui(label: str):
    return(
        ui.div(
            ui
        )
    )